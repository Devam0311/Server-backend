# Use pnpm for package management
package-manager=pnpm

# Skip optional dependencies to reduce build time
optional=false

# Skip dev dependencies in production
production=true

# Disable scripts that might cause build issues
ignore-scripts=false 